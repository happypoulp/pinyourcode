Install sass & compass:

gem install sass
gem install compass

Compile scss to css:

# sass --watch public/css/sass:public/css/gen

DEV:

compass watch .
nodemon app.js

SUBLIME:

"folder_exclude_patterns": [".sass-cache"]